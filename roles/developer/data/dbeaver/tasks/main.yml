---

#################################################################

- name: "apt | install"
  apt:
    name    : "{{ libraries_apt }}"
    state   : "latest"
  tags:
    - "developer"
    - "data"
    - "dbeaver"

#################################################################

- name: "apt | download and install deb"
  apt:
    deb     : "{{ dbeaver.url }}"
  tags:
    - "developer"
    - "data"
    - "dbeaver"

#################################################################

- name: "copy | desktop icon"
  template:
    src         : "{{ dbeaver.desktop }}.desktop.j2"
    dest        : "/usr/share/applications/{{ dbeaver.desktop }}.desktop"
    owner       : "{{ primary_user.id }}"
    group       : "{{ primary_user.id }}"
    mode        : "0644"
  tags:
    - "developer"
    - "data"
    - "dbeaver"
    - "launcher"

- name    : "cmd | unity | add to launcher"
  when    :
    - ansible_distribution      | lower == "ubuntu"
    - ansible_lsb.major_release | int   <  18
  become  : "{{ primary_user.id }}"
  script  : "{{ launcher_favourite.unity }}"
  tags:
    - "developer"
    - "data"
    - "dbeaver"
    - "launcher"

- name    : "cmd | gnome | add to launcher"
  when    :
    - ansible_distribution      | lower == "ubuntu"
    - ansible_lsb.major_release | int   >= 18
  become  : "{{ primary_user.id }}"
  script  : "{{ launcher_favourite.gnome }}"
  tags:
    - "developer"
    - "data"
    - "dbeaver"
    - "launcher"

#################################################################

