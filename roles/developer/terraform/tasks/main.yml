---

#################################################################

- name: "mkdirs"
  file:
    name    : "{{ item }}"
    state   : "directory"
    recurse : "yes"
    owner   : "{{ primary_user.id }}"
    group   : "{{ primary_user.id }}"
    mode    : "0755"
  with_items:
    - "{{ terraform.home }}/{{ terraform_version }}"
  tags:
    - developer
    - terraform

#################################################################

- name: "apt | install"
  apt:
    name    : "{{ item }}"
    state   : "latest"
  with_items: "{{ libraries_apt }}"
  tags:
    - developer
    - terraform

#################################################################

- name: "unpack | application"
  unarchive:
    src         : "{{ terraform.url }}{{ terraform.zip }}"
    dest        : "{{ terraform.home }}/{{ terraform_version }}"
    remote_src  : "yes"
    creates     : "{{ terraform.home }}/{{ terraform_version }}/terraform"
  tags:
    - developer
    - terraform

#################################################################

