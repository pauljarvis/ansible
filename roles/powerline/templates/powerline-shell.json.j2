{
    {{ note.json -}}
{% if not gui | bool %}
    "mode": "flat",
{% endif %}
    "theme": "default",
    "segments": [
        "ssh",
        "username",
        "cwd",
        "git",
        "git_stash"
    ],
    "cwd": {
        "mode": "plain"
    }
}

